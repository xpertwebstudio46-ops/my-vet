import { CalendarDays, Images, PhoneCall, UsersRound } from 'lucide-react'
import type { Practice } from '@/lib/api/types'

export function PublicPracticeDetails({ practice }: { practice: Practice }) {
  const pricing = practice.pricing ?? []
  const gallery = practice.galleryMedia ?? []
  const team = practice.teamMembers ?? []
  const holidays = (practice.holidayHours ?? []).filter((item) => new Date(item.date) >= new Date(new Date().toISOString().slice(0, 10)))
  const emergency = practice.emergencyHours

  return <div className="flex flex-col gap-8">{pricing.length > 0 && <section><h2 className="mb-4 text-2xl font-bold text-[#0d2e5e]">Pricing</h2><div className="grid gap-4 sm:grid-cols-2">{pricing.map((item) => <article key={item.id} className="rounded-xl border border-slate-200 bg-white p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wide text-[#13b8a8]">{item.kind === 'HEALTH_PACKAGE' ? 'Health package' : item.section}</p><h3 className="mt-1 font-semibold text-slate-900">{item.name}</h3></div><strong className="whitespace-nowrap text-lg text-[#064071]">{formatPrice(item.price, item.currency)}</strong></div>{item.description && <p className="mt-2 text-sm text-slate-500">{item.description}</p>}{item.billingPeriod && item.billingPeriod !== 'ONE_OFF' && <p className="mt-2 text-xs text-slate-500">Billed {item.billingPeriod.toLowerCase()}</p>}</article>)}</div></section>}{gallery.length > 0 && <section><h2 className="mb-4 flex items-center gap-2 text-2xl font-bold text-[#0d2e5e]"><Images className="size-5 text-[#13b8a8]" />Gallery</h2><div className="grid gap-4 sm:grid-cols-2">{gallery.map((media) => media.mediaType === 'VIDEO' ? <video key={media.id} controls className="aspect-video w-full rounded-xl border bg-black" src={media.url} /> : <figure key={media.id} className="overflow-hidden rounded-xl border bg-white"><img src={media.url} alt={media.altText ?? media.caption ?? `${practice.name} gallery image`} className="aspect-video w-full object-cover" />{media.caption && <figcaption className="p-3 text-sm text-slate-500">{media.caption}</figcaption>}</figure>)}</div></section>}{team.length > 0 && <section><h2 className="mb-4 flex items-center gap-2 text-2xl font-bold text-[#0d2e5e]"><UsersRound className="size-5 text-[#13b8a8]" />Meet the team</h2><div className="grid gap-4 sm:grid-cols-2">{team.map((member) => <article key={member.id} className="flex gap-4 rounded-xl border bg-white p-4">{member.imageUrl ? <img src={member.imageUrl} alt={member.name} className="size-20 shrink-0 rounded-xl object-cover" /> : <span className="flex size-20 shrink-0 items-center justify-center rounded-xl bg-teal-50 text-2xl font-semibold text-teal-700">{member.name.slice(0, 1)}</span>}<div><h3 className="font-semibold">{member.name}</h3><p className="text-sm font-medium text-[#13b8a8]">{member.role}</p>{member.qualifications && <p className="mt-1 text-xs text-slate-500">{member.qualifications}</p>}{member.bio && <p className="mt-2 text-sm text-slate-600">{member.bio}</p>}</div></article>)}</div></section>}{(emergency?.enabled || holidays.length > 0) && <section className="grid gap-4 sm:grid-cols-2">{emergency?.enabled && <article className="rounded-xl border bg-white p-5"><h2 className="flex items-center gap-2 font-semibold text-[#0d2e5e]"><PhoneCall className="size-5 text-[#13b8a8]" />Emergency contact</h2>{emergency.phone && <a href={`tel:${emergency.phone.replace(/[^+\d]/g, '')}`} className="mt-3 block font-semibold text-[#064071]">{emergency.phone}</a>}{emergency.instructions && <p className="mt-2 whitespace-pre-line text-sm text-slate-500">{emergency.instructions}</p>}</article>}{holidays.length > 0 && <article className="rounded-xl border bg-white p-5"><h2 className="flex items-center gap-2 font-semibold text-[#0d2e5e]"><CalendarDays className="size-5 text-[#13b8a8]" />Upcoming holiday hours</h2><ul className="mt-3 space-y-2">{holidays.map((holiday) => <li key={holiday.id} className="text-sm text-slate-600"><strong>{new Intl.DateTimeFormat('en-GB', { dateStyle: 'medium', timeZone: 'UTC' }).format(new Date(holiday.date))}</strong> — {holiday.isClosed ? 'Closed' : `${holiday.opensAt}–${holiday.closesAt}`}{holiday.note ? ` · ${holiday.note}` : ''}</li>)}</ul></article>}</section>}</div>
}

function formatPrice(price: string, currency: string) {
  try { return new Intl.NumberFormat('en-GB', { style: 'currency', currency }).format(Number(price)) } catch { return `${currency} ${price}` }
}
